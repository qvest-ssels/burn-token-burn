# CLAUDE.md

Read `AGENTS.md` — it is the single set of rules for coding agents in this repository (environment via `uv`, read-only against tool data, stdlib-only, one ADR per assistant, run `token-finops self-audit` before opening a PR).
